import UIKit

struct Core {
    static let apiManager = APIManager()
    static let passwordField = CodePayTextField(frame: .zero)
    static let confirmPasswordField = CodePayTextField(frame: .zero)
}
